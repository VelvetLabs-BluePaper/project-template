# {{PROJECT_NAME}}

VelvetLabs Project Template. Use `gh repo create --template VelvetLabs-BluePaper/project-template` to create a new project.

After cloning, run the setup script to replace placeholders:

```bash
./scripts/setup-project.sh "My Project" "my-repo" "Next.js + TypeScript"
```
